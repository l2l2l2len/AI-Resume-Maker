import React from 'react';

export const ConfigError: React.FC = () => {
  // This component is not actively used since the Gemini API dependency was removed.
  // It is kept to prevent import errors in the application structure.
  // Returning null ensures it renders nothing if it were to be included.
  return null;
};
